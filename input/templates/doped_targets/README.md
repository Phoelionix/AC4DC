The folder `targets/` contains template files intended to be used to generate batches of files using AC4DC/generate_batch.py

To generate a batch of simulation files for each target in the folder, run `bash doped_batch_generation.sh`. Batches are generated according to parameters set in `AC4DC/generate_batch.py`